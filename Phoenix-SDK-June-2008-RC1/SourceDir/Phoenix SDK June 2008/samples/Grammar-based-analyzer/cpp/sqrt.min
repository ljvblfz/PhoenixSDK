sqrt(int a, int result)
{
   if (a >= result*result) 
      Print(result);
   else
      sqrt(a, result-1);
}

Main()
{
   n = 123;
   Print("The square root of ", n, " is:");
   sqrt(n, n/2);
}
